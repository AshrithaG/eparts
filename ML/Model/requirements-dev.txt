# Developer-only tooling. Not installed in production containers.
-r requirements.txt

ruff>=0.4,<0.13
black>=24.0,<26.0
mypy>=1.10,<2.0
types-PyYAML>=6.0.12
pandas-stubs>=2.1

# Test fixtures: synthesize text-bearing PDFs at test time so the pdfplumber
# path can run without committing binary fixtures or installing tesseract.
reportlab>=4.0,<5.0
