# Runtime — pinned for reproducibility.
# Update via a deliberate PR; bumping a version is a design-review event.

# --- Data foundation (M1) ---
pandas>=2.1,<3.0
pyarrow>=14.0,<19.0
numpy>=1.26,<3.0
scikit-learn>=1.4,<2.0           # Ledoit-Wolf shrinkage covariance (Layer 3)

# --- Layer 1 (extraction) ---
pdfplumber>=0.11,<0.12            # PDF text extraction
pytesseract>=0.3.10,<0.4          # OCR fallback (requires system tesseract binary)
Pillow>=10.0,<12.0                # image handling for OCR path
PyYAML>=6.0,<7.0                  # config files

# --- Layer 2 (rule engine) ---
rapidfuzz>=3.6,<4.0               # manufacturer fuzzy match

# --- Layer 3 (semantic matcher) ---
sentence-transformers>=3.0,<5.0   # bge-small-en-v1.5 encoder
torch>=2.2,<3.0                   # CPU build sufficient (no fine-tuning in V1)
faiss-cpu>=1.8,<2.0               # IVFFlat index over 1B

# --- Layer 4 / service ---
filelock>=3.12,<4.0               # cross-platform lock for online μ updates (M6)
fastapi>=0.110,<1.0               # REST endpoint (M7)
uvicorn>=0.27,<1.0
prometheus-client>=0.20,<1.0      # CAP-ML-04 telemetry (M7)

# --- Tests ---
pytest>=8.0,<9.0
